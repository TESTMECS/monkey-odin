#!/usr/bin/env monkey -- 1,2,3

# class Point {
# 	let new = fn(self, x, y) {
# 		self.x = x;
# 		self.y = y;
# 	};
# };
#
# let p = Point.new(1, 2);

foreach i in {"a": 1, "b": 2, "c": 3} {
	puts(i);
}

